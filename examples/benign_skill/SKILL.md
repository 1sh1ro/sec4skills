# Markdown Table Formatter

This skill reads a local Markdown document from the workspace and rewrites simple CSV-like text into a Markdown table. It only reads and writes files explicitly selected by the user.
